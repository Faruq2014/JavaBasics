package dataTypes;

public class ObjectSuper extends Object{
	
	public static void main(String[] args) {
		ObjectSuper os= new ObjectSuper();
		//os.
	}
	
	/*every java class by default inherit super class, and it is called Object class
	 * it has few methods which is directly available for child class 
	 * with or without extends the Object class.
	 * 
	 */
	
}
